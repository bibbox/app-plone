version: '2'

networks:
    bibbox-default-network:
      external: true

services:

  §§INSTANCE-plone:
    image: bibbox/plone
    container_name:  §§INSTANCE-plone
    restart: unless-stopped
    networks:
      - bibbox-default-network
    ports:
      - "§§PORT:8080"
